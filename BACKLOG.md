# Backlog

1. [x] Scaffolding — Set up monorepo structure with Express server (src/server/) and React SPA (src/client/), Vite + TypeScript build, shadcn/ui + Tailwind CSS + Lucide icons + Sonner, health endpoint at GET /api/health, install and configure vitest + testing-library for frontend and vitest for backend, add npm test script, one placeholder test passing per layer <!-- depends: none -->
2. [x] Azure client infrastructure — Install Azure SDKs (@azure/storage-blob, @azure/cosmos, @azure/identity), read STAMP_ID env var (default "qqq"), derive storage account name (stautodev{stampId}) and Cosmos account (cosmos-autodev-{stampId}), initialize DefaultAzureCredential, create shared Azure client modules, Cosmos DB database/container createIfNotExists on startup (autodev db, items container, partition key /organizationId) <!-- depends: 1 -->
3. [x] App shell and routing — Sidebar + main content layout using shadcn/ui sidebar components, "AutoDev" logo heading, navigation grouped under "Projects" (Dashboard) and "Admin" (Sample Specs) with Lucide icons, active page highlighting, React Router with routes for /, /projects/new, /projects/:id, /admin/sample-specs, responsive sidebar (persistent desktop, hamburger overlay mobile), content area with max-w-5xl centering <!-- depends: 1 -->
4. [x] Projects API and dashboard — Project entity model (id, organizationId, type, name, specName, createdAt, latestRunStatus, runCount), projects service with Cosmos DB queries (always filter organizationId="default", single-partition), GET /api/projects (list, sorted by createdAt desc), GET /api/projects/:id, Dashboard page at / listing projects in table (desktop) or cards (mobile) with name and relative created date, clickable rows navigating to /projects/:id, "New Project" button, empty/loading/error states with skeleton and retry <!-- depends: 2, 3 -->
5. [x] New project creation — POST /api/projects route that creates Cosmos DB record and blob container (idempotent container creation), New Project page at /projects/new with name field (required, max 100 chars, auto-focused) and sample spec picker (Select dropdown loading from GET /api/sample-specs, shows filenames without .md extension), React Hook Form + Zod validation, inline errors, submit with spinner, cancel button, navigate to /projects/:id on success, toast on error <!-- depends: 4 -->
6. [x] Project detail and log viewer — GET /api/projects/:id/logs route reading .log files or events.jsonl from project's blob container, project detail page at /projects/:id showing project name, created date, back-to-dashboard link, terminal-style log viewer (dark bg, monospace, font-mono) with 5-10s polling, auto-scroll that pauses on manual scroll up and resumes at bottom, pause/resume toggle button with pulsing dot indicator, responsive layout (full-height desktop, wrapped mobile with text-xs), empty/loading/error states <!-- depends: 4 -->
7. [x] Sample specs API and admin page — Sample specs service for blob storage CRUD on sample-specs container, GET /api/sample-specs (list with name, size, lastModified), GET /api/sample-specs/:name (content), POST /api/sample-specs (upload .md file), DELETE /api/sample-specs/:name, Admin page at /admin/sample-specs with table (filename, human-readable size, last modified, actions) or mobile card layout, upload button triggering native file picker (.md only, multi-select), delete with AlertDialog confirmation, view modal (Dialog with raw markdown in scrollable pre block, download button), toast notifications, empty/loading/error states, auto-refresh after upload/delete <!-- depends: 2, 3 -->
