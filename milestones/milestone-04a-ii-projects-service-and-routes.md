# Milestone 04a-ii — Projects service and API routes

## Milestone: Projects Cosmos DB service and API routes

> **Validates:** `npm run build` succeeds. `npm start` starts the server. `GET /api/projects` returns HTTP 200 with a JSON array (empty array `[]` when no projects exist). `GET /api/projects/nonexistent-id` returns HTTP 404 with JSON `{ "message": ... }`. `npm test` passes all existing tests.

> **Reference files:** `src/server/models/project.ts` (Project interface — created in milestone 04a-i), `src/server/services/sampleSpecs.ts` (existing service pattern — async functions, try/catch, descriptive error messages), `src/server/routes/sampleSpecs.ts` (existing route pattern — Router export, thin handlers calling service, JSON error envelope), `src/server/azure/cosmosClient.ts` (shared CosmosClient instance), `src/server/config.ts` (exports `cosmosDatabaseName` and `cosmosContainerName` — use these instead of hardcoded strings), `src/server/app.ts` (where to register new router with `app.use("/api", ...)` before the API 404 handler).

## Tasks

- [x] Create projects service at src/server/services/projectsService.ts — import `cosmosClient` from "../azure/cosmosClient.js", import `cosmosDatabaseName` and `cosmosContainerName` from "../config.js", import `Project` from "../models/project.js"; get container reference via `cosmosClient.database(cosmosDatabaseName).container(cosmosContainerName)`; export async function `listProjects(): Promise<Project[]>` that runs a parameterized SQL query `SELECT TOP 100 * FROM c WHERE c.organizationId = @orgId AND c.type = "project" ORDER BY c.createdAt DESC` with parameter `@orgId` = "default", calls `fetchAll()`, returns `resources`; wrap in try/catch with descriptive error `Failed to list projects: ${message}`
- [ ] Add getProjectById to src/server/services/projectsService.ts — export async function `getProjectById(id: string): Promise<Project | null>` that calls `container.item(id, "default").read<Project>()`, returns `resource` if found; in the catch block, detect Cosmos 404 by checking `(err as { code?: number | string }).code === 404 || (err as { code?: number | string }).code === "NotFound"` and return `null` for not-found, re-throw all other errors with descriptive message `Failed to get project "${id}": ${message}`
- [ ] Create projects router at src/server/routes/projects.ts — import Router from "express", import `listProjects` and `getProjectById` from services; export `projectsRouter`; GET `/projects` calls `listProjects()` and returns JSON array with 200; GET `/projects/:id` calls `getProjectById(id)`, returns 404 JSON `{ message: "Project not found" }` if null, otherwise returns project JSON with 200; catch blocks must log the error with `console.error` before returning 500 JSON `{ message: "Internal server error" }`
- [ ] Register projects router in src/server/app.ts — import `projectsRouter` from "./routes/projects.js", add `app.use("/api", projectsRouter)` after the sampleSpecsRouter registration and before the API 404 handler
- [ ] Update .github/copilot-instructions.md — add Key files entries for `src/server/models/project.ts` (Project entity interface with Cosmos DB document fields), `src/server/services/projectsService.ts` (projects service with list and get-by-id Cosmos queries, always filters organizationId="default"), `src/server/routes/projects.ts` (projects API routes for list and get-by-id)
