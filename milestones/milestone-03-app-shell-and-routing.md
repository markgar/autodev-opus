# Milestone 03 — App Shell and Routing

## Milestone: Sidebar layout, navigation, React Router, and responsive shell

> **Validates:** `npm run build` succeeds. `npm start` starts the server. `GET /` returns HTTP 200 with HTML containing "AutoDev" heading in the sidebar. The SPA renders a sidebar with two navigation groups: "Projects" containing "Dashboard" and "Admin" containing "Sample Specs". Navigating to `/projects/new` renders a page with heading "New Project". Navigating to `/projects/test-123` renders a page with heading "Project Detail". Navigating to `/admin/sample-specs` renders a page with heading "Sample Specs". The sidebar contains an "AutoDev" logo/heading. A `SidebarTrigger` hamburger button is present for mobile. `npm test` passes all existing tests.

> **Reference files:** `src/client/App.tsx` (root React component to modify for routing), `src/client/main.tsx` (React entry point), `src/client/lib/utils.ts` (cn() class merge utility), `components.json` (shadcn/ui CLI configuration for installing sidebar component), `.github/copilot-instructions.md` (documentation to update with new key files).

## Tasks

- [x] Fix findings #22 and #23: update .github/copilot-instructions.md — fix Key files descriptions for `src/client/lib/utils.ts` to "CSS class name merge utility for component styling" and `src/client/test-setup.ts` to "Frontend test setup adding DOM assertion matchers" (describe roles, not implementation details)
- [x] Install react-router-dom: `npm install react-router-dom`
- [x] Add shadcn/ui sidebar component via CLI (`npx shadcn@latest add sidebar`) — this generates sidebar primitives in src/client/components/ui/sidebar.tsx (SidebarProvider, Sidebar, SidebarHeader, SidebarContent, SidebarGroup, SidebarGroupLabel, SidebarGroupContent, SidebarMenu, SidebarMenuItem, SidebarMenuButton, SidebarTrigger, etc.)
- [x] Create placeholder page components in src/client/pages/: DashboardPage.tsx (renders heading "Projects" inside a div), NewProjectPage.tsx (renders heading "New Project"), ProjectDetailPage.tsx (renders heading "Project Detail" and reads `:id` from useParams), SampleSpecsPage.tsx (renders heading "Sample Specs") — each is a simple exported functional component with just a page heading
- [x] Create AppSidebar component at src/client/components/AppSidebar.tsx — uses Sidebar, SidebarHeader, SidebarContent, SidebarGroup, SidebarGroupLabel, SidebarGroupContent, SidebarMenu, SidebarMenuItem, SidebarMenuButton from @/components/ui/sidebar; SidebarHeader renders "AutoDev" as a styled heading (text-lg font-semibold px-2); two SidebarGroups: first with label "Projects" containing a Dashboard menu item (LayoutDashboard icon from lucide-react, links to /), second with label "Admin" containing a Sample Specs menu item (FileText icon from lucide-react, links to /admin/sample-specs); uses React Router Link for navigation; uses useLocation to determine active item and sets isActive prop on SidebarMenuButton when pathname matches
- [x] Create AppLayout component at src/client/components/AppLayout.tsx — wraps page content in SidebarProvider containing AppSidebar and a main element; the main element contains a header bar with SidebarTrigger button (hamburger icon, visible on mobile via md:hidden class), and a content div with className "max-w-5xl mx-auto px-4 md:px-8 py-6" for centered, responsive content; renders React Router Outlet inside the content div for child route pages
- [x] Wire up React Router in src/client/App.tsx — replace current placeholder content with BrowserRouter wrapping Routes; define a parent Route with path "/" and element={<AppLayout />} containing child routes: index element → DashboardPage, path "projects/new" → NewProjectPage, path "projects/:id" → ProjectDetailPage, path "admin/sample-specs" → SampleSpecsPage; keep Sonner Toaster rendered outside the router
- [ ] Update .github/copilot-instructions.md — add Key files entries: `src/client/components/AppSidebar.tsx` (sidebar navigation with grouped menu items and active highlighting), `src/client/components/AppLayout.tsx` (root layout wrapping sidebar and routed page content), note `src/client/pages/` directory as convention for page-level route components; update Architecture section to mention client-side routing via React Router with BrowserRouter
